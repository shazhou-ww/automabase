/**
 * Tests for sample-counter app
 */

import { describe, expect, it } from 'vitest';

describe('sample-counter app', () => {
  it('should be a sample app for testing', () => {
    // This is a sample app, minimal tests
    expect(true).toBe(true);
  });
});
