{
  "name": "@automabase/stack-automabase",
  "version": "1.0.0",
  "description": "Automabase Lambda Stack - HTTP API and WebSocket functions",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "build": "bun run build:functions",
    "build:functions": "bun scripts/build-functions.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@automabase/automata-auth": "workspace:*",
    "@automabase/automata-core": "workspace:*",
    "@aws-sdk/client-apigatewaymanagementapi": "^3.695.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^20.11.0"
  }
}
