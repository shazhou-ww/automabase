{
  "name": "@automabase/function-jsonata-eval",
  "version": "1.0.0",
  "description": "Lambda function: jsonata-eval",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node24 --format=cjs --outfile=dist/index.js --external:\"@aws-sdk/*\" --minify --sourcemap",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "jsonata": "^2.1.0"
  },
  "devDependencies": {}
}
