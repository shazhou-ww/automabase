{
  "name": "@automabase/jsonata-eval-client",
  "version": "1.0.0",
  "description": "Client package for invoking jsonata-eval Lambda function",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "clean": "echo 'No dist to clean'"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}

