{
  "name": "@automabase/automata-auth",
  "version": "1.0.0",
  "description": "Shared package: automata-auth",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "clean": "echo 'No dist to clean'"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.965.0",
    "@aws-sdk/client-dynamodb": "^3.965.0",
    "@aws-sdk/lib-dynamodb": "^3.965.0",
    "@noble/ed25519": "^2.2.3",
    "jose": "^6.1.3"
  }
}
